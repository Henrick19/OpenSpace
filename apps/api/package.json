{
  "name": "@openspace/api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Node.js integration, upload and SQLite service for the PSB dashboard",
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "node --check src/server.js",
    "start": "node src/server.js",
    "lint": "oxlint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@openspace/shared": "*",
    "better-sqlite3": "^12.6.2",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "vitest": "^4.1.0"
  }
}
