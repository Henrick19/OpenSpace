export function NewCapturePage() {
  return (
    <section>
      <h1 className="h2">New capture</h1>
      <p className="text-secondary">This page will contain project/floor selection, capture verification and the INSV upload workflow.</p>
      <div className="alert alert-info">Page owner: build the form here and call functions from <code>src/services</code>. Do not call OpenSpace directly from React.</div>
    </section>
  );
}
