# Docker placeholder

Docker is planned first for the Node.js integration and upload service. Runnable Dockerfiles and Compose configuration will be added only when the application has package manifests, a committed lockfile and server source code.

See [Docker setup plan](../docs/deployment/DOCKER_SETUP.md) and [planned dependencies](../docs/development/DEPENDENCIES.md).
